name: Release installers

# Tag a version to ship downloadable apps for every OS:
#   git tag v0.1.0 && git push origin v0.1.0
# Installers (Windows .exe, macOS .dmg, Linux .AppImage/.deb) are built on
# real OS runners and attached to the GitHub Release automatically.
# Also dispatchable by hand (Actions tab → Release installers → Run workflow):
# untagged runs attach the installers to a DRAFT release named after the
# version in package.json — review it and click Publish.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
          - os: macos-latest
          - os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run smoke
      - name: Build and publish installers
        run: npx electron-builder --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
